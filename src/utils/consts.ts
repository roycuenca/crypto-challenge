export enum OperationType {
  BUY = 'BUY',
  SELL = 'SELL',
}

export enum OrderType {
  LIMIT = 'LIMIT',
  MARKET = 'MARKET',
}

export enum CryptoCurrency {
  BTC = 'BTC',
  ETH = 'ETH',
  USDC = 'USDC',
}

export enum DefaultCurrency {
  USDT = 'USDT',
}

export enum OrderStatus {
  PENDING = 'PENDING',
  EXECUTED = 'EXECUTED',
}

export enum platformFee {
  FEE = 0.015,
}
